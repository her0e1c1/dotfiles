# dotfiles repository instructions

- When adding dependencies, packages or external-tool-related settings whose purpose is not obvious from the code alone, you must add a short comment that explains why they are needed
  - Example: `Required for nvim to render Nerd Font glyphs correctly`
