interface:
  display_name: "新規テスト計画"
  short_description: "code から不足していそうな test case を探す"
  default_prompt: "$plan-new-tests を使って implementation と現在の tests を読み、欠けていそうな test cases と短い follow-up plan を list 化する。"
