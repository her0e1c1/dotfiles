
HISTORY_DIRECTORY=~/.history_directory

push () {
    if [ -d $1 ]; then
      if [ ! -e $HISTORY_DIRECTORY ]; then
            touch $HISTORY_DIRECTORY
      fi
      echo $1 >> $HISTORY_DIRECTORY
      cd $1
    fi
}

dir () {
    cat $HISTORY_DIRECTORY
}